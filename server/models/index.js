/**
 * @author nkuma15
 */
//registry for all the models that are needed for use: Create index of Models here
module.exports = {
  userprofile: require('./UserProfile.js')//db.contactlist.find().pretty()
};